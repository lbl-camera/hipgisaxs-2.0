#include <array>
#include <execution>
#include <complex>

template <typename T>
std::complex<T> coneff(const std::vector<T> &qx,
    const std::vector<T> &qy,
    const std::vector<T> &qz,
    T R0, T R1, T h,
    

    // find-angle
    T tan_a = H / (R0 - R1);

}
